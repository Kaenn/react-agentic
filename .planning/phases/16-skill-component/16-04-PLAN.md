---
phase: 16-skill-component
plan: 04
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/index.ts
autonomous: true

must_haves:
  truths:
    - "Skill, SkillFile, SkillStatic components exported from index.ts"
    - "emitSkill, emitSkillFile functions exported from index.ts"
    - "SkillDocumentNode type exported from index.ts"
  artifacts:
    - path: "src/index.ts"
      provides: "Public API exports for skill components"
      contains: "Skill.*SkillFile.*SkillStatic"
  key_links:
    - from: "src/index.ts"
      to: "src/jsx.ts"
      via: "re-export"
      pattern: "export.*from.*jsx"
---

<objective>
Export Skill components and types from the library's public API.

Purpose: Enable consumers to import Skill, SkillFile, SkillStatic from react-agentic
Output: Complete public API for skill authoring
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-skill-component/16-02-SUMMARY.md

# Existing export pattern
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Skill exports to src/index.ts</name>
  <files>src/index.ts</files>
  <action>
Update src/index.ts to export the new Skill components and functions.

1. Add to JSX component exports (where Command, Agent, etc. are exported):
```typescript
export {
  // ... existing exports ...
  Skill,
  SkillFile,
  SkillStatic,
  type SkillProps,
  type SkillFileProps,
  type SkillStaticProps,
} from './jsx.js';
```

2. Add to emitter exports (where emit, emitAgent are exported):
```typescript
export {
  // ... existing exports ...
  emitSkill,
  emitSkillFile,
} from './emitter/emitter.js';
```

3. Add to IR type exports (where DocumentNode, AgentDocumentNode are exported):
```typescript
export type {
  // ... existing exports ...
  SkillDocumentNode,
  SkillFrontmatterNode,
  SkillFileNode,
  SkillStaticNode,
} from './ir/index.js';
```

Follow the existing export pattern in the file - look at how Command/Agent are exported and mirror that structure for Skill.
  </action>
  <verify>Run `npx tsc --noEmit` - no type errors in index.ts</verify>
  <done>Skill components, emitter functions, and IR types exported from public API</done>
</task>

</tasks>

<verification>
```bash
# Type check passes
npx tsc --noEmit

# Skill exports present
grep -n "Skill\|emitSkill\|SkillDocumentNode" src/index.ts
```
</verification>

<success_criteria>
- [ ] Skill, SkillFile, SkillStatic components exported
- [ ] SkillProps, SkillFileProps, SkillStaticProps types exported
- [ ] emitSkill, emitSkillFile functions exported
- [ ] SkillDocumentNode, SkillFrontmatterNode, SkillFileNode, SkillStaticNode types exported
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-skill-component/16-04-SUMMARY.md`
</output>
